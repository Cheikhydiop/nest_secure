// jwt-payload.interface.ts
export interface JwtPayload {
    phone: string;  // Le numéro de téléphone de l'utilisateur
    // Vous pouvez ajouter d'autres champs comme l'ID de l'utilisateur ou le rôle ici
  }
  